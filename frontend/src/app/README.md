This is the root page.

Subpages are created automatically from folders in this directory.
For example the folder `cat` would result in the page `/cat`.

The page.tsx file is where the react code for the page is stored.